services:
  chessdotcom-ai-coach:
    build: .
    env_file:
      - .env
    environment:
      # Override host names to match the compose network.
      - POSTGRES_HOST=postgres
      - OLLAMA_HOST=http://ollama:11434
      - CHESS_ENGINE_HOST=chess-engine
      - CHESS_ENGINE_PORT=3333
    ports:
      - "8000:8000"
    depends_on:
      chess-engine:
        condition: service_started
      postgres:
        condition: service_healthy
      ollama:
        condition: service_healthy

  chess-engine:
    build:
      context: chess_engine
    ports:
      - "3333:3333"

  postgres:
    image: postgres:18-alpine
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=password
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 5s

  ollama:
    build:
      context: ollama
    env_file:
      - .env
    ports:
      - "11434:11434"
    volumes:
      - ollama-data:/root/.ollama
    healthcheck:
      test: ["CMD-SHELL", "ollama list | grep -q \"$${OLLAMA_MODEL:-llama3:8b}\""]
      interval: 10s
      timeout: 5s
      retries: 60
      start_period: 10s

volumes:
  postgres-data:
  ollama-data:
